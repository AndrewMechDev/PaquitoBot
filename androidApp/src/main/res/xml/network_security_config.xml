<?xml version="1.0" encoding="utf-8"?>
<!--
  Permite HTTP plano (sin TLS) SOLO hacia el backend local de pruebas
  `canvas-mock` (ver skill `canvas-mock-backend`) - Android bloquea
  cleartext por default desde API 28. Todo el resto de la app (Google,
  paquitobot-rag) sigue exigiendo HTTPS - no se relaja globalmente.
  IP LAN + 10.0.2.2 (emulador) + localhost, cubre los dos escenarios de
  prueba. Esta config es temporal, igual que canvas-mock: se borra cuando
  ya no haga falta.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">192.168.1.113</domain>
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
</network-security-config>
